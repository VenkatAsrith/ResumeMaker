# Frontend Environment Variables

# API URL - leave empty to use relative path (recommended for same-origin deployment)
# Set this only if your backend is on a different domain
VITE_API_URL=

# Example for different backend:
# VITE_API_URL=https://your-backend-api.com/api
